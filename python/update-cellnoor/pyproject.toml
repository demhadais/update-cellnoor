[project]
name = "update-cellnoor"
description = "An app that reads JSON files (representing sample-tracking sheets at the SCBL) and converts them into API requests for cellnoor"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
